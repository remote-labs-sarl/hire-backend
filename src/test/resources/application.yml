spring:
  application:
    name: remotelabs-hire
  datasource:
    url: jdbc:tc:postgresql:15.2:///hire_db
  test:
    database:
      replace: none
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
  flyway:
    enabled: false